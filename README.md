# Wei-py Tap

## How do I install these formulae?

`brew install wei-py/tap/<formula>`

Or `brew tap wei-py/tap` and then `brew install <formula>`.

Or, in a `brew bundle` `Brewfile`:

```ruby
tap "wei-py/tap"
brew "<formula>"
```

## Documentation

`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).
